# Wesley's Blog

This a simple Full Stack blog web app. It was built using MongoDB, Express, Node.js and uses the Semantic UI framework for styling.

Features include Viewing all posts and being able to Delete/Edit posts.

### To Start the app

You will need to have node installed as well as a mongo server running.

Then you run node app.js then connect to http://localhost:3000./